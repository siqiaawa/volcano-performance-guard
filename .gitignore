.work/
tmp/
*.tmp
__pycache__/
*.py[cod]

# Large Release assets and the extracted stable checkout stay outside Git.
release-assets/*.tar
release-assets/*.tar.gz
release-assets/*.part
stable/volcano/
runtime/benchmark-assets/benchmark-images.tar
runtime/benchmark-assets/imported-manifest.json
runtime/benchmark-assets/registry-images.env

# Reports are run artifacts. Keep only the directory policy document.
reports/*
!reports/README.md
